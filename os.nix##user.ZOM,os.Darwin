{
  name = "ZOM";
  home = "/Users/ZOM";
  system = "aarch64-darwin";
  module = { lib, pkgs, ... }: {
    programs.gpg.enable = true;
    programs.emacs.package = lib.mkForce (pkgs.emacs30.override { withNativeCompilation = false; });
  };
}
